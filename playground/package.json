{
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "nodemon -w '../src/**/*.ts' -e .ts -x vite dev",
    "build": "nodemon -w '../src/**/*.ts' -e .ts -x vite build"
  },
  "devDependencies": {
    "vite": "^4.0.0",
    "vite-plugin-inspect": "^0.7.10"
  }
}